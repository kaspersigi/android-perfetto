# actionlint 1.7.12 does not yet list the hosted Ubuntu 26.04 public preview.
# This only extends lint's accepted labels; the workflow is NOT self-hosted.
# https://github.com/actions/runner-images/blob/main/images/ubuntu/Ubuntu2604-Readme.md
self-hosted-runner:
  labels:
    - ubuntu-26.04
